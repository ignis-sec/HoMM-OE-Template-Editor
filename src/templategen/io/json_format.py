"""JSON formatting helpers — indentation, key order, trailing newline."""


def dumps(data: object) -> str:
    raise NotImplementedError


def loads(text: str) -> object:
    raise NotImplementedError
